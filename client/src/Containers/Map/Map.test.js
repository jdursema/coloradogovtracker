import {DataMap} from './Map'
import React from 'react'
import {shallow} from 'enzyme'
import {mockStateTotals} from '../../mockdata/mockStateTotals.js'

describe('Map tests', () => {
  let mockStateData;

  // beforeEach(() => {
  //   mockStateData = mockStateTotals
  //   renderedMap = shallow(<DataMap data = {mockStateData} />)
  // })


  it('should render a map', () => {
 
  })
})